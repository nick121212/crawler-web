export default (models) => {
    // models.pergroup.hasMany(models.pergroupaction, {
    //     foreignKey: 'group_id',
    //     constraints: false,
    //     as: 'groupActions'
    // });

    // models.action.belongsToMany(models.pergroupaction, {
    //     foreignKey: 'action_id',
    //     constraints: false
    // });
}